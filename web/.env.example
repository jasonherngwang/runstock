# API base URL (Worker). Use your deployed Worker URL in production.
# For local dev: run `pnpm dev` (wrangler) and set this to the port shown (e.g. http://localhost:8787 or 8789).
NEXT_PUBLIC_API_URL=http://localhost:8787
